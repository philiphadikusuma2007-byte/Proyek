package Model.SkillsFile;
import Model.StatusEffectFile.*;
import Model.ElementsFile.*;

public class RockThrow extends AttackSkill{
    
    public RockThrow(){
        super("Rock Throw", new Earth(), 45, new None(), 0);
    }
}
