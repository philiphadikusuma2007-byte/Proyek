package Model.SkillsFile;
import Model.StatusEffectFile.*;
import Model.ElementsFile.*;

public class DarkClaw extends AttackSkill{
    
    public DarkClaw(){
        super("Dark Claw", new Dark(), 70, new None(), 0);
    }
}
