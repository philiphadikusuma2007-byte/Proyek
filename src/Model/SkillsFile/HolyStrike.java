package Model.SkillsFile;
import Model.StatusEffectFile.*;
import Model.ElementsFile.*;

public class HolyStrike extends AttackSkill{
    
    public HolyStrike(){
        super( "Holy Strike", new Light(), 70, new None(), 0);
    }
}
