package Model.SkillsFile;
import Model.StatusEffectFile.*;
import Model.ElementsFile.*;

public class FireClaw extends AttackSkill{
    
    public FireClaw(){
        super("Fire Claw", new Fire(), 50, new None(), 0);
    }
}
