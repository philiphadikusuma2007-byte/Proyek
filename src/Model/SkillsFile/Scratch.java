package Model.SkillsFile;
import Model.StatusEffectFile.*;
import Model.ElementsFile.*;

public class Scratch extends AttackSkill{
    
    public Scratch(){
        super( "Scratch", new Earth(), 40, new None(), 0);
    }
}
