package Model.SkillsFile;
import Model.*;
import Model.StatusEffectFile.*;
import Model.ElementsFile.*;

public class RockSmash extends AttackSkill {
    public RockSmash() {
        super( "Rock Smash", new Earth(), 45, new None(), 30);
    }
}
